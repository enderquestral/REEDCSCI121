#hanheller
#no partner, due to being sick on the lab day

"""takes two number parameters a and b. 
It should give back a linear function that uses those numbers as its coefficients. 
The parameter a should be the slope of that linear function, and the b should be the offset."""

def linear(a,b):
    return lambda c: (a*c) +b